# Local working notes, not part of the workshop material
.scratch/
charter.md

# OS / editor cruft
.DS_Store

# Local account selection; machine-specific
.envrc
.direnv/

# Terraform working state; preflight leaves it in the attendee's clone
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
